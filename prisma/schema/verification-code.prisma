model VerificationCode {
  id        String   @id @default(uuid())
  email     String
  dbCode    String   @map("DBcode")
  expiresAt DateTime
  used      Boolean  @default(false)
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
